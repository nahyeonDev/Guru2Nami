<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.guru2nami">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Guru2Nami">
        <activity android:name=".MapMenu.MapActivity"></activity>
        <activity android:name=".MypageContent.SalesHistoryActivity" />
        <activity android:name=".MypageContent.BuyHistoryActivity" />
        <activity android:name=".User.SignInActivity" />
        <activity android:name=".User.LoginActivity" />
        <activity android:name=".Content.TogetherWriteActivity" />
        <activity android:name=".Content.PersonalWriteActivity" />
        <activity android:name=".Content.DetailViewActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".IntroSplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="wsvvqgm6u5" />
    </application>

</manifest>